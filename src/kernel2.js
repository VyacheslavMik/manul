'use strict';

const CHAR_SPACE = 32;
let memory = [];
let ds           = { arr: [], p: 0, desc: "Data stack", limit: 1024 };
let rs           = { arr: [], p: 0, desc: "Return stack", limit: 1024 };

function mainLoop() {
    
}
